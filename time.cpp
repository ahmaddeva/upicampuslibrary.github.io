#include <iostream>
#include <time.h>

using namespace std;

int main()
{
    // time_t ct=time(0);
    // string currenttime = ctime(&ct);
    cout << __DATE__ << endl;

    cin.get();
    return 0;
}